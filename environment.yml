name: neuro-agent
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - poetry
  - pytorch
  - numpy
  - pandas
  - jupyterlab
  - transformers
  - pyarrow
  - tokenizers
  - datasets
  - pip:
      - langchain
      - sentence-transformers
      - rdflib
      - networkx
      - sympy
